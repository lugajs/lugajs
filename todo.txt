- Consider creating a Bower package, maybe an NPM one too
- Try istanbul for measuring test coverage
- Spin-off for jasmine-ducktype?

-------------

DOCS:

- Move into GitHub pages?
- Talk about CORS?

- Use a datepicker to document custom trait?
- Sample of dataSet/binding to a third-party widget?
- Region scripting and API?

-------------

AJAXFORM:

- Parse and handle error messages from server
- Use live service for samples and docs
- Standardize JSON schema for error messages

-------------
CSI:
-------------

CORE:

- Implement luga.string.queryToHash()

- Document luga.Notifier
- Document luga.extend()

-------------
VALIDATOR:
-------------

DATA:

data.Dataset:

- Implement formatter function (passed to constructor)
- Implement update(filter, setter)
- Allow custom sorter for complex objects
- Local storage persistence? It would require serialization of full dataSet (including filter! Pass filters as function names?)

data.PagedDataset:

- Implement

data.region.Base:

- Apply traits at currentRowchanged? All of them or just selected? Metadata or different args inside the constructor?

-------------

DATA WIDGETS:

- ShowMore widget
  10 records at time (fetchedRecords)
  Once done change button's label
  Mixed client/server (set a threshold)

- Pagination widget (needs PagedDataset)
- Navbar widget (needs PagedDataset)
- Autosuggest widget

- Use string templating to customize url/endpoint

-------------

DATA FUTURE:

- Add it to default Gulp task

- data.Rss2Dataset
  https://validator.w3.org/feed/docs/rss2.html

-------------
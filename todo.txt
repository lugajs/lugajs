- Move from JSHint to ESlint
- Update Bootstrap to 4.x then update jQuery to 3.x

- Add second, optional argument to addObserver(observer, eventName)

-------------

DOCS:

- Luga.data: examples of region with event delegation:
  https://api.jquery.com/on/#direct-and-delegated-events
  https://learn.jquery.com/events/event-delegation/

- Add page for luga.dom

-------------

AJAXFORM:

- Make JSON serializer configurable
- Parse and handle error messages from server
- Use live service for samples and docs
- Standardize JSON schema for error messages

-------------
COMMON:
-------------

HISTORY:

- Encode/decode fragment?

- Write documentation:
  Implication of using pushState regarding the server: https://github.com/flatiron/director#history-api
  Explain how to use pushState on top of SEO <a> links

- Add to distro

-------------
VALIDATOR:
-------------

DATA:

luga.data.DataSet:
- Local storage persistence

luga.data.PagedData:
- Mixed client/server (options.threshold)
- Use string templating to customize url/endpoint
- Request: offsetStart, offsetEnd
- Response: recordCount, pageSize, currentPage

-------------

DATA WIDGETS:

luga.data.widgets.PagingBar (needs PagedData)
- Different GUI styles: Bootstrap, PageList, LinkList

luga.data.widgets.Autosuggest
  https://heroes.dev.waylon.cloud/v1/search/character?offset=0&limit=10&name=byss

luga.data.widgets.Tags

-------------
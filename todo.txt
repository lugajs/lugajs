- Consider creating a Bower package

-------------

DOCS:

- Switch from em to rem
- Use flexbox CSS
- Review/improve luga.css
- Layout forms with labels on top of fields

-------------

AJAXFORM:

- Parse and handle error messages from server
- Standardize JSON schema for error messages

-------------
CSI:
-------------

CORE:

- Implement luga.string.queryToHash()

- Document luga.Notifier?
- Document luga.extend()?

-------------
VALIDATOR:
-------------

DATA:

data.Dataset:

- Fix getContext() and binding to region/template

- update(filter, setter)

- Implement state (empty?, loading, error, ready)
- Implement column types (with getters/setters)
- Implement sorting (with getters/setters)

- Local storage persistence? It would require serialization of full dataSet


data.DetailSet:

- Write unit test!
- Improve master/detail sample page. Use setCurrentRowId()


data.HttpDatset:


data.JsonDataSet:

- Add .rawJson to store raw JSON
- .getRawJson()
- .setRawJson()

- Modify loadRecords() to handle more complex JSON structures like Spry?
- Implement subPaths?


data.Region:

- Write unit test!
- Throw exception if dataSource is not found
- Investigate external template files

- Add ability to define dataset programmatically, not only with custom attribute
- Validate required attributes

- Add custom attributes:
  data-lugads-setrowid
  data-lugads-setrowindex
  data-lugads-state
  data-lugads-sort

-------------

DATA FUTURE:

- Once mature. Add it to default Gulp task
- Implement pagination
- Implement navbar widget

data.Autosuggest:

- Implement widget

data.XmlDataset:

- options.xpath
- setXpath()
- getXpath()
- loadRecords()

-------------